"""
Gateway package initializer.

This module makes the `gateway.app` directory a Python package so that it can be
imported as `app` when running under Uvicorn.  It does not currently perform any
initialisation logic, but the file is required by Python packaging conventions.
"""